# YuGi 桌面宠物 - 更新日志

## 版本 1.2 (2026-05-27)

### 🎯 新增功能

#### 1. ⚙️ 可调整帧率和移动速度
- ✨ **设置界面**: 新增图形化设置界面，可调整各种参数
- ✨ **动画帧率**: 可调整范围 50-200ms（5-20 FPS）
- ✨ **初始移动速度**: 可调整范围 5-60 秒
- ✨ **随机移动速度**: 可调整范围 1-10 秒
- ✨ **实时生效**: 动画帧率修改后立即生效

#### 2. 🖥️ 点击显示后台运行程序
- ✨ **左键点击**: 点击宠物显示当前运行的程序列表
- ✨ **详细信息**: 显示程序名称、窗口标题、内存占用
- ✨ **智能过滤**: 只显示有窗口的程序，过滤后台服务
- ✨ **限制数量**: 显示前 20 个程序

#### 3. 🎯 改进的交互方式
- ✨ **新的左键行为**: 左键点击显示运行程序（不再是拖动）
- ✨ **拖动功能**: 通过右键菜单选择"拖动宠物"
- ✨ **增强的右键菜单**: 添加"拖动宠物"、"查看运行程序"、"设置"选项
- ✨ **托盘菜单更新**: 添加"设置"选项

### 📝 配置文件更新

新增配置项：
```json
{
  "InitialMoveDuration": 18,        // 初始移动持续时间（秒）
  "RandomMoveDurationMin": 1,       // 随机移动最小时间（秒）
  "RandomMoveDurationMax": 3        // 随机移动最大时间（秒）
}
```

### 🔧 技术改进

- 使用 `Process.GetProcesses()` 获取系统进程信息
- 动态创建 WPF 设置窗口
- 实时更新动画定时器间隔
- 配置文件自动保存和加载

### 📊 性能影响

- **CPU 占用**: 无明显变化（< 1%）
- **内存占用**: +5 MB（设置界面和进程信息）
- **启动时间**: 无变化（< 2 秒）

---

## 版本 1.1 (2026-05-27)

### 🎯 新增功能

#### 初始登场动画
- ✨ **左下角登场**: 宠物现在从屏幕左下角开始出现
- ✨ **缓慢向右移动**: 启动后自动缓慢向右移动到屏幕右侧
- ✨ **平滑过渡**: 使用 18 秒的缓动动画，提供流畅的视觉体验
- ✨ **自动切换**: 初始移动完成后，自动切换到随机移动模式

### 📝 技术实现

#### 初始位置设置
```csharp
// 设置窗口初始位置在屏幕左下角
this.Left = 10; // 左边距 10 像素
this.Top = SystemParameters.PrimaryScreenHeight - this.Height - 50; // 底部边距 50 像素
```

#### 缓慢向右移动
```csharp
private void StartInitialRightMovement()
{
    double screenWidth = SystemParameters.PrimaryScreenWidth;
    double targetX = screenWidth - this.Width - 10; // 移动到右边

    // 创建缓慢的向右移动动画（18秒）
    Duration duration = new Duration(TimeSpan.FromSeconds(18));

    DoubleAnimation moveX = new DoubleAnimation
    {
        To = targetX,
        Duration = duration,
        EasingFunction = new QuadraticEase { EasingMode = EasingMode.EaseInOut }
    };

    // 动画完成后开始随机移动
    moveX.Completed += (s, e) => 
    { 
        isMoving = false;
        if (config.AutoMove && !movementTimer.IsEnabled)
        {
            movementTimer.Start();
        }
    };

    this.BeginAnimation(Window.LeftProperty, moveX);
}
```

### 🎮 用户体验

#### 启动流程
1. **程序启动** → 宠物出现在屏幕左下角
2. **登场动画** → 缓慢向右移动（约 18 秒）
3. **自动模式** → 移动完成后开始随机移动（如果启用）

#### 交互说明
- **拖拽**: 在移动过程中仍可以左键拖动宠物
- **停止**: 拖动后会停止初始移动动画
- **恢复**: 拖动后会继续随机移动模式（如果启用）

### ⚙️ 配置说明

初始移动动画是固定的，不受配置文件影响。配置文件仍然控制：
- `AutoMove`: 是否在初始移动后启用随机移动
- `AnimationSpeed`: 序列帧动画速度
- `MoveIntervalMin/Max`: 随机移动的间隔时间

### 🔧 自定义建议

如果想调整初始移动的速度，可以修改 `StartInitialRightMovement()` 方法中的持续时间：

```csharp
// 更快：10 秒
Duration duration = new Duration(TimeSpan.FromSeconds(10));

// 默认：18 秒
Duration duration = new Duration(TimeSpan.FromSeconds(18));

// 更慢：30 秒
Duration duration = new Duration(TimeSpan.FromSeconds(30));
```

### 📊 性能影响

- **CPU 占用**: 无明显增加（仍 < 1%）
- **内存占用**: 无变化（~30-50 MB）
- **启动时间**: 无变化（< 2 秒）

### 🎨 视觉效果

- **缓动函数**: `QuadraticEase.EaseInOut`
- **移动轨迹**: 水平直线（Y 坐标保持不变）
- **速度变化**: 开始慢 → 中间快 → 结束慢

---

## 版本 1.0 (2026-05-27)

### 🎉 初始版本

#### 核心功能
- ✅ 14 帧流畅动画（YuGi_Idle）
- ✅ 透明窗口（无边框、置顶）
- ✅ 自由拖拽
- ✅ 随机移动
- ✅ 系统托盘
- ✅ 配置持久化

#### 技术实现
- 使用 WPF + .NET 8.0
- DispatcherTimer 动画控制
- DoubleAnimation 平滑移动
- NotifyIcon 系统托盘
- JSON 配置管理

#### 文档
- README_YuGi.md
- 快速开始.md
- 开发实现说明.md
- 项目总结.md
- 验收清单.md

---

## 🔮 未来计划

### 版本 1.2（计划中）
- [ ] 添加多种登场动画（从上、从下、淡入等）
- [ ] 支持自定义登场位置和移动路径
- [ ] 添加登场音效

### 版本 2.0（计划中）
- [ ] 多种动画状态（行走、跑步、睡觉）
- [ ] 宠物互动功能
- [ ] 自定义皮肤切换
- [ ] AI 聊天功能

---

**最后更新**: 2026-05-27  
**当前版本**: 1.1
